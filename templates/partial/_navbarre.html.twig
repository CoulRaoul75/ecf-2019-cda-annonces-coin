<nav class="navbar navbar-expand-lg" style="background-color: #0c5460" >

    <a class="navbar-brand text-white mr-5" href="{{ path('ad-list') }}"><i class="fas fa-home"></i></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">

        <ul class="navbar-nav">

            <li class="nav-item active mr-5">
                <a class="nav-link text-white mr-xl-5" href="{{ path('ad-new') }}">Déposer une annonce <i
                            class="fas fa-plus-square"></i><span class="sr-only">(current)</span></a>
            </li>

            <li class="nav-item active mr-5">
                <a class="nav-link text-white mr-xl-5" href="{{ path('user-new') }}">Créer un compte <i class="fas fa-user"></i><span
                            class="sr-only">(current)</span></a>
            </li>

            <li class="mr-5">
                <form class="form-inline">
                    <input class="form-control mr-sm-2" type="search" placeholder="Rechercher..." aria-label="Search">
                    <button class="btn btn-outline-success my-2 my-sm-0" type="submit"><i class="fas fa-search"></i>
                    </button>
                </form>
            </li>


            <li class="nav-item dropdown mr-5">
                <a class="nav-link dropdown-toggle text-white mr-3" href="#" id="navbarDropdown" role="button"
                   data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    Catégories <i class="fas fa-feather-alt"></i>
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                    {% for category in categoryList %}
                        <a class="dropdown-item " href="{{ path('show-by-category', {'id': category.id}) }}">
                            {{ category.titleCategory }} <span class="badge badge-info badge-pill">
                        <h8>{{ category.ads | length }}</h8>
                        </span>
                        </a>
                    {% endfor %}
                    <div class="dropdown-divider"></div>
                    <a class="dropdown-item" href="{{ path('ad-list') }}">Toutes les catégories</a>
                </div>
            </li>

            {% if app.user.username is defined %}
                <li class="nav-item navbar-text text-white mr-2">
                    {{ app.user.username }}
                </li>
                <li class="nav-item">
                    <a class="nav-link text-white" href="{{ path('logout') }}">Se deconnecter <i class="fas fa-sign-out-alt"></i></a>
                </li>
            {% else %}
                <li class="nav-item">
                    <a class="nav-link text-white" href="{{ path('security-login') }}">Se connecter <i
                                class="fas fa-sign-in-alt"></i></a>
                </li>
            {% endif %}

        </ul>
    </div>
</nav>
